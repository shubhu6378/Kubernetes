---
apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    env: dev
    type: frontend
spec:
  containers:
    - name: nginx-container
      image: nginx
      ports:
        - containerPort: 80

